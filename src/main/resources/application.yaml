spring:
  application:
    name: Receipt-OCR
  datasource:
    url: jdbc:postgresql://localhost/Receipt?user=postgres&password=postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true